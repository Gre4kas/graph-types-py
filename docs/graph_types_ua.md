# Типи графів: Повний посібник

Вибір правильного представлення графа — це перший крок до успішного моделювання. Ця бібліотека надає чотири типи графів для покриття будь-яких математичних та практичних потреб.

---

## 🧭 Як обрати правильний тип графа

Використовуйте цю матрицю, щоб визначити, який клас найкраще підходить для вашого проєкту:

| Обмеження | SimpleGraph | Multigraph | Pseudograph | Hypergraph |
| :--- | :---: | :---: | :---: | :---: |
| **Петлі** (A → A) | ❌ Ні | ❌ Ні | ✅ Так | N/A |
| **Паралельні ребра** (A ⇉ B) | ❌ Ні | ✅ Так | ✅ Так | ✅ Так |
| **Арність** (Вершин на ребро) | Рівно 2 | Рівно 2 | Рівно 2 | Будь-яка (2, 3+) |
| **Представлення за замовчуванням** | Adjacency List | Multi-List | Multi-List | Incidence Map |

---

## 1. SimpleGraph (Простий граф)
*Стандартний математичний граф: $G = (V, E)$.*

### 🛠️ Архітектура
`SimpleGraph` оптимізований для швидкості та математичної чистоти. Він суворо контролює, щоб жодна вершина не з'єднувалася сама з собою, а між двома вершинами існувало лише одне ребро.

### 🚀 Посібник з використання
```python
from packages.graphs import SimpleGraph

# 1. Ініціалізація
# representation може бути: "adjacency_list" (default) або "adjacency_matrix"
graph = SimpleGraph(directed=False, representation="adjacency_list")

# 2. Управління вершинами
graph.add_vertex("Kyiv", population=2800000)
graph.add_vertex("Lviv", region="West")

# 3. Управління ребрами
graph.add_edge("Kyiv", "Lviv", weight=540) # O(1) avg

# 4. Обмеження (Безпека)
try:
    graph.add_edge("Kyiv", "Kyiv") # Викличе GraphConstraintError
except:
    print("Петлі заборонені в SimpleGraph!")
```

---

## 2. Multigraph (Мультиграф)
*Фокус на відносинах: Кілька з'єднань між точками.*

### 🛠️ Архітектура
`Multigraph` дозволяє паралельні ребра. Кожному ребру присвоюється унікальний внутрішній ID, що дозволяє зберігати різні атрибути для кожного з'єднання між тими самими вузлами.

### 🌟 Унікальні методи
- `edge_multiplicity(u, v)`: Повертає кількість ребер між `u` та `v`.
- `get_edges_between(u, v)`: Повертає список об'єктів `Edge`.

### 🚀 Посібник з використання
```python
from packages.graphs import Multigraph

multi = Multigraph()
multi.add_vertex("Account1")
multi.add_vertex("Account2")

# Кілька транзакцій між одними й тими самими рахунками
multi.add_edge("Account1", "Account2", amount=100, date="2024-01-01")
multi.add_edge("Account1", "Account2", amount=250, date="2024-01-02")

print(f"Транзакції: {multi.edge_multiplicity('Account1', 'Account2')}") # Вивід: 2
```

---

## 3. Pseudograph (Псевдограф)
*Фокус на топології: Циклічні посилання та складні стани.*

### 🛠️ Архітектура
`Pseudograph` — найгнучкіший вибір для бінарних ребер. Він успадковує функціонал `Multigraph`, але знімає обмеження на петлі.

### 🌟 Унікальні методи
- `has_self_loop(v)`: Перевірка наявності петлі для конкретної вершини.
- `count_self_loops()`: Загальна кількість петель у графі.
- `total_degree(v)`: Обчислює ступінь, де петлі рахуються як **2**.

### 🚀 Посібник з використання
```python
from packages.graphs import Pseudograph

pseudo = Pseudograph()
pseudo.add_vertex("Process_A")

# Стан обслуговування: процес посилається на самого себе
pseudo.add_edge("Process_A", "Process_A", status="stabilizing")

print(pseudo.total_degree("Process_A")) # Вивід: 2 (правильна поведінка згідно з теорією графів)
```

---

## 4. Hypergraph (Гіперграф)
*Фокус на множинах: Групова співпраця та складні зв'язки.*

### 🛠️ Архітектура
У `Hypergraph` ребра — це не пари, а множини. Використовуйте цей тип, коли "зв'язок" включає 3 або більше сутностей одночасно (наприклад, груповий чат, молекула або технічний комітет).

### 🌟 Унікальні методи
- `add_hyperedge({"A", "B", "C"})`: Створення багатовершинного з'єднання.
- `get_incident_hyperedges(v)`: Пошук усіх "груп", до яких належить вершина.
- `to_bipartite_graph()`: Конвертація в `SimpleGraph` для візуалізації.

### 🚀 Посібник з використання
```python
from packages.graphs import Hypergraph

hyper = Hypergraph()
users = ["Alice", "Bob", "Charlie", "David"]
for u in users: hyper.add_vertex(u)

# Створення команди проєкту з 3 користувачів
hyper.add_hyperedge({"Alice", "Bob", "Charlie"}, project="AI_Model")

# В яких проєктах бере участь Alice?
projects = hyper.get_incident_hyperedges("Alice")
```
